services:
  dc-reverse-proxy:
    image: ghcr.io/svenfinn/dc-reverse-proxy
    container_name: dc-reverse-proxy
    hostname: reverse_proxy
    build: .
    ports:
      - "${APP_PORT}:80"
    networks:
      - displaycontroller
