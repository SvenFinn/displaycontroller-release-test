{
  "name": "dc-db-local",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "postinstall": "ls; npx prisma generate",
    "prepack": "chmod +x ./pack.sh && ./pack.sh"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^5.17.0",
    "prisma": "^5.17.0"
  },
  "devDependencies": {
    "@types/node": "^22.1.0",
    "typescript": "^5.5.4"
  },
  "files": [
    "dist",
    "schema.prisma",
    ".env"
  ],
  "prisma": {
    "schema": "schema.prisma"
  }
}